# Oauth simple test authentication

## Pre-requisites
- client-id and secret-key on github.

Or generate a different oauth server with the properties accordingly

## Build
mvn clean install

## Install / Usage
mvn spring-boot:run